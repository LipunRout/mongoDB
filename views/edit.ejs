<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Backend</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

</head>
<body>
  <div class="w-full h-full bg-zinc-900 p-10 text-white">

<div class="nav pb-10">
  <a href="/read" class="text-blue-500 ">Read Users</a>
</div>

    <h1 class="text-3xl tracking-tighter mb-3">Update User</h1>
    <form action="/update/<%= users._id %>" method="post">
      <input class="px-5 border-2 rounded-lg border-zinc-800 py-2 outline-none bg-transparent"  type="text" placeholder="name" name="name" value="<%= users.name %>">
      <input class="px-5 border-2 rounded-lg border-zinc-800 py-2 outline-none bg-transparent"  type="text" placeholder="email" name="email" value="<%= users.email %>" >
      <input class="px-5 border-2 rounded-lg border-zinc-800 py-2 outline-none bg-transparent"  type="text" placeholder="image url" name="image" value="<%= users.image %>">
      <input type="submit" value="Update user" class="px-5 py-2 bg-yellow-500 text-black rounded-lg ml-3">
    </form>
  </div>
</body>
</html>